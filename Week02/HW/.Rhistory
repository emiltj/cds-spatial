library(sf)
library(raster)
library(tmap)
library(spData)
library(spDataLarge)
library(pacman)
p_load(raster, sf, tmap, spData, spDataLarge)
tm_shape(nz_elev)  +
tm_raster(title = "elev",
style = "cont",
palette = "BuGn") +
tm_shape(nz) +
tm_borders(col = "red",
lwd = 3) +
tm_scale_bar(breaks = c(0, 100, 200),
text.size = 1) +
tm_compass(position = c("LEFT", "center"),
type = "rose",
size = 2) +
tm_credits(text = "A. Sobotkova, 2020") +
tm_layout(main.title = "My map",
bg.color = "orange",
inner.margins = c(0, 0, 0, 0))
library(pacman)
p_load(raster, sf, tmap, spData, spDataLarge)
library(pacman)
p_load(raster, sf, tmap, spData, spDataLarge)
install.packages(BiocManager)
install.packages("BiocManager")
p_load(raster, sf, tmap, spData, spDataLarge)
install.packages("spDataLarge")
tm_shape(nz_elev)  +
tm_raster(title = "elev",
style = "cont",
palette = "BuGn") +
tm_shape(nz) +
tm_borders(col = "red",
lwd = 3) +
tm_scale_bar(breaks = c(0, 100, 200),
text.size = 1) +
tm_compass(position = c("LEFT", "center"),
type = "rose",
size = 2) +
tm_credits(text = "A. Sobotkova, 2020") +
tm_layout(main.title = "My map",
bg.color = "orange",
inner.margins = c(0, 0, 0, 0))
p_load(raster, sf, tmap, spData, spDataLarge)
library(pacman)
p_load(sf, raster, tmap, spData, spDataLarge)
